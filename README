docker-compose up --build

Команда docker-compose exec web python manage.py csu создаст суперпользователя для
входа на страницу администрирования проекта (по умолчанию логин 'admin@sky.pro',пароль 'hghghg777')

При запуске программы необходимо создать файл .env,
в котором указать данные для переменных окружения. Образец для заполнения находится в файле .env_example